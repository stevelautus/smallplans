{
  "description": "smallplans coordination gate — injects standing ledger obligations when the current project has entries",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/coord-gate.sh"
          }
        ]
      }
    ]
  }
}
